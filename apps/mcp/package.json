{
  "name": "@submail/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "dev:http": "SUBMAIL_MCP_TRANSPORT=http tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "start:http": "SUBMAIL_MCP_TRANSPORT=http node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.4",
    "tsx": "^4.20.3"
  }
}
